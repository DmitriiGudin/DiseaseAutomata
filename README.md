Currently the version is very raw. You can run the following commands:

'python simulation.py' - run the simulation based on the map *maps/test_map.json*.

'python editor.py' - run the editor to create your own map. Make sure to call it *test_map* (it will replace the existing map).
