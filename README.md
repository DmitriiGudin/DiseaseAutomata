Currently the version is very raw. You can run the following commands.


Run the simulation based on the map *maps/test_map.json*: ```python simulation.py``` 


Run the editor to create your own map: ```python editor.py``` Make sure to call it *test_map* (it will replace the existing map).
